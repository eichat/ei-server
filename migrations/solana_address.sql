-- Адреса гаманця Solana в профілі.
--
-- Навіщо саме адреса, а не гаманець усередині застосунку: так ми не торкаємось
-- приватних ключів узагалі. Людина називає, КУДИ надсилати токени; підписує
-- транзакції її власний гаманець. Це не робить нас зберігачем чужих активів —
-- а зберігання одразу означало б регуляторні вимоги (Україна — закон про
-- віртуальні активи, ЄС — MiCA) і відповідальність за кожен злам.
alter table public.users add column if not exists solana_address text;
